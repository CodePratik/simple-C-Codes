#include<stdio.h>
#include<conio.h>
int main(){
    int num;
    printf("Enter the Number less than 10");
    scanf("%d",&num);
    if(num<=10)
    printf("What an obedient servant you are!");
    getch();
}